// Set this before building for a hosted game server, for example:
// window.GAME_SERVER_URL = 'https://your-server.example.com';
// For local testing on an Android emulator use: http://10.0.2.2:3000
window.GAME_SERVER_URL = window.GAME_SERVER_URL || '';
